// ---- Fill these in with your own GitHub details, then commit this file ----
const CONFIG = {
  GITHUB_OWNER: "aaadam-H",       // your GitHub username
  GITHUB_REPO: "internship-logbook", // the repo this site lives in
  GITHUB_BRANCH: "main",             // branch the site deploys from
  DATA_PATH: "data.json"             // path to the data file in the repo
};
